const raio = 3; //raio da base
const altura = 1; //altura do cilindro

//função areaTotal = 2 * pi * raio * (raio + altura)
const areaTotal = 2 * 3.14 * raio * (raio + altura);

console.log("A área total do cilindro é de", areaTotal, "PI");
